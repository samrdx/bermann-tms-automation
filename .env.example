# Bermann TMS QA Automation - Environment Variables Template
# Copy this file to .env and fill with your credentials

# TMS Environments
BASE_URL_DEV=https://moveontruckqa.bermanntms.cl
BASE_URL_STAGING=https://moveontruckqa.bermanntms.cl
BASE_URL_PROD=https://moveontruck.bermanntms.cl

# Configuration
ENVIRONMENT=dev
HEADLESS=false
TIMEOUT=30000

# Logging
LOG_LEVEL=info

# Test Users (TMS QA/Demo Environment)
TMS_USERNAME=example    
TMS_PASSWORD=example

# CI/CD Alternative Names (GitHub Actions uses these)
# USERNAME_DEV=example
# PASSWORD_DEV=example

# Engram Memory Store (optional, for SDD hybrid mode)
ENGRAM_BASE_URL=http://localhost:8080
# Optional: only needed if your Engram endpoint is protected by a proxy/auth layer
ENGRAM_API_KEY=
ENGRAM_PROJECT=bermann-tms-automation
ENGRAM_HEALTH_PATH=/health

# DO NOT COMMIT .env TO GIT
# This template (.env.example) is safe to commit
# Your actual .env with real credentials should stay local
